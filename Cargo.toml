[workspace]
resolver = "2"

members = ["native/arm_test", "native/arm", "native/counter_nif"]

# Always optimize; building and running the guest takes much longer without optimization.
[profile.dev]
opt-level = 3

[profile.release]
debug = 1
lto = true
